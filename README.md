# Trip_Planner
# Hey People
My first commit message
# configuration
git config user.email "email"
git config user.name "username"
# create a token
Settings->Dev Settings->Personal Access Tokens->classic->generate a token->all select

# commit changes form git bash

$ notepad Requirements(file_name)

2.update & save notepad file

3.cat Requirements(modified or not)

4.git add Requirements(file_name)

5.git status (green-Modified)

6.git commit -m "MSG"

7.git push
